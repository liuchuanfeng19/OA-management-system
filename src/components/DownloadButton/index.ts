import downloadButton from "./src/index.vue";
import { withInstall } from "@pureadmin/utils";

/** 文件下载按钮组件 */
export const DownloadButton = withInstall(downloadButton);

export default DownloadButton;
